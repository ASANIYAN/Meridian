# REST API origin (the /v1 prefix is added by the API client, not here).
VITE_API_URL=http://localhost:8000

# WebSocket gateway origin. Runs on its own port (WS_PORT, default 8001),
# separate from the REST server. The provider authenticates via ?token=.
VITE_WS_URL=ws://localhost:8001
