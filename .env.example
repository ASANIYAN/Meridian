# The WS gateway rides this same origin/port now — no separate WS_PORT or
# VITE_WS_URL. The provider derives ws://(same host) or wss://(same host) from
# this value and authenticates via ?token=.
VITE_API_URL=http://localhost:8000
